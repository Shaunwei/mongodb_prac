#!/usr/bin/env python
import pymongo

HOST = 'localhost'
PORT = 27017

def input_data(client, filename):
    
    with open(filename, 'rb') as f:
        line = f.readline()
        while(line):
            
            
